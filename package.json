{
  "name": "xpro-howtomovetheneedle",
  "private": false,
  "version": "0.1.0",
  "description": "Optional MIT starter: X signals (bookmarks, likes, follows, replies, outbound DMs) → private Lists for pro.x.com. Free host pool or BYO X app.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply xpro-htmtn --local",
    "db:migrate:remote": "wrangler d1 migrations apply xpro-htmtn --remote",
    "ship": "bash scripts/ship.sh",
    "secrets:x-oauth": "bash scripts/put-x-oauth-secrets.sh"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
